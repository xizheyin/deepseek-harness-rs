# Compatibility status

This table is the source of truth for claims about compatibility with DeepSeek Harness commit `47f943859bef60e4160492346772ded9b24f765a`. Status values are:

- `unresearched`: upstream behavior has not been studied in enough detail;
- `planned`: upstream behavior and a Rust design exist, but implementation does not;
- `partial`: only part of the behavior is implemented;
- `compatible`: deterministic comparison tests support the claim;
- `intentional-difference`: the difference, reason, impact, and tests are recorded.

The v0.1 release gate covers rows in the shipped Phase 0–9 product scope. Rows
whose area begins with `Post-v0.1` deliberately track broader hardening goals;
they remain honest `partial` work without being advertised as v0.1 behavior.

Phase 53 expands the current enhanced command palette from the fifteen entries
named in the older Phase 11 row below to sixteen and adds three local PTY
journeys, for 91 real PTY journeys in that evidence family.

| Area | Upstream evidence | Rust implementation | Comparison evidence | Status | Known difference |
| --- | --- | --- | --- | --- | --- |
| CLI launcher | `apps/cli/src/{args,bin,profile-boot,process-shutdown}.ts` and launcher tests recorded in `docs/upstream.md` | `src/main.rs` delegates to the real `src/cli/` entry; the closed surface supports help/version, one interactive terminal Session, `--prompt`, and piped input | Argument/unit tests plus real-binary `cli_smoke` and PTY suites | `intentional-difference` | Official `dsh` launches profile/plugin/configuration and Web/headless products and reports usage errors as exit 1. Rust v0.1 is one standalone terminal agent, deliberately rejects those modes and positional tasks, and reports usage errors as exit 2. Scripts written for the official launcher are therefore not CLI-compatible; help/version/usage sentinels prove rejected startup does not open a workspace, read a key, or connect to the model endpoint. |
| Phase 1 provider-neutral value vocabulary | `packages/llm/llm/src/{brand,message,types,call-config}.ts`, `packages/attachment/attachment/src/types.ts` | `src/model/` and `src/json_value.rs` | Type-checked `modelVocabulary` in the generated oracle; `tests/model_types.rs` | `compatible` | Scoped to serializable Phase 1 values: IDs, messages/sources/blocks, image references, failures, finish reasons, token usage, chunks, config, adapter defaults, and tool schemas. Whole-stream grammar belongs to Phase 2. |
| Session header, canonical known event envelopes, seed, and replay | `packages/core/session/src/{types,index,json}.ts`, `tests/{session,properties}.spec.ts` | `src/session/{event,codec,mod}.rs` | Generated canonical, preservation, forward-compatibility, and illegal fixtures; `tests/session_codec.rs` | `compatible` | Scoped to values emitted by the official typed producers. The compact `{header,events}` snapshot is an in-memory interchange and test seam, not the upstream or Phase 8 durable JSONL storage format. |
| Malformed fields inside known event vocabulary | Raw upstream Session performs minimum shell checks for several payloads | Rust decodes current known fields into validated types before replay | Oracle `knownPayloadAdmission`; `malformed_current_vocabulary_is_a_fail_closed_import_difference` | `intentional-difference` | Official raw append/load can retain values such as `maxTokens:null`, non-string stop items, or an incomplete tool schema. Rust rejects them at import so a later provider cannot receive a half-typed request. Custom/plugin logs using malformed current field names may fail Rust import; unknown extra fields and future merge-extensible variants remain preserved. The paired test fixes both outcomes. |
| Model-visible surface and request projections | `packages/core/session/src/{surface,request-header,index}.ts`, `tests/{surface,request-header}.spec.ts` | `src/session/projection.rs` | Generated append/replace, numeric rewrite, header/context fixtures; `tests/{session_core,session_request_projection}.rs` | `compatible` | Compatibility covers canonical surface folding and latest request projections. Producing compaction replacements and durable transcript/UI projections are later phases. |
| Turn, step, and tool-call relations with the official companion enabled | `packages/core/session/src/invariant.ts`, `tests/invariant.spec.ts` | Always-on checks in `src/session/projection.rs` | Generated valid/illegal outcomes; `tests/session_core.rs` | `compatible` | Same observable acceptance/rejection when the official invariant companion is installed. |
| Invariant registration and Cordis lifecycle | Bare `SessionStore` behavior versus installed `SessionInvariant` in the generated oracle | One owned `Session`; relations are always enabled and there is no Cordis scope/HMR/observer registry | `invariantRegistration` oracle section; `always_on_rust_invariant_is_a_verified_architecture_difference` | `intentional-difference` | Official custom producers can omit/dispose the companion and then append malformed relations. Rust cannot disable the checks because one safe CLI-owned session is easier to audit. Such malformed plugin traces are rejected; canonical logs and the wire schema are unchanged. The paired oracle/Rust test locks the difference. |
| Unknown required event import policy | Bare core accepts arbitrary plugin event types; persistence coordinator rejects unknown required types and preserves unknown `ignorable: true` events | The Phase 1 snapshot import adopts the persistence rule early | Oracle `upstreamBareCoreUnknownRequired`; coordinator source review; `unknown_required_events_fail_but_unknown_ignorable_events_are_preserved` | `intentional-difference` | Rust rejects an unrecognized event unless `ignorable:true`; ignorable events are retained losslessly. This prevents an older CLI from silently replaying required semantics it cannot understand. A raw plugin log accepted by bare upstream core may therefore fail Rust import until its event type is implemented or marked ignorable. |
| Bounded JSON/session admission | Upstream accepts any finite JSON number other than negative zero and has no equivalent fixed Phase 1 size/count caps | Bounded JSON plus 16 MiB snapshot/retained-payload budget, 8 MiB value, 64 KiB header, 128 levels, one million nodes, 4096 events/provenance entries; integral opaque values must be JavaScript-safe | Oracle `numericBoundary`; `tests/{model_types,session_limits}.rs` | `intentional-difference` | JavaScript rounds some integers beyond `2^53-1`; Rust rejects them instead of silently changing evidence. Oversized but otherwise valid official in-memory logs are also rejected. This is a fail-closed resource policy for untrusted CLI input; it can block importing very large/plugin-heavy logs but never changes an accepted event. Tests lock the numeric and every size/count rejection plus atomic recovery. The 16 MiB wire/payload cap is not a promise that peak process RSS is 16 MiB. |
| JSON object property order | Upstream objects preserve JavaScript enumeration order, and some later request-change logic uses `JSON.stringify` | `serde_json::Map` canonicalizes object keys; semantic equality ignores object order | Deterministic encode tests; model fixture compares parsed JSON values | `intentional-difference` | Rust snapshots are semantically stable but not guaranteed byte-identical to an upstream `JSON.stringify` result whose input used another key order. This improves deterministic Rust serialization but can reorder extension/schema keys. Phase 3 must use explicit field-wise request equivalence, not raw JSON or derived `Eq`, when deciding whether to append a new request header. |
| DeepSeek model preparation and retry facts | `packages/llm/llm/src/{index,retry-policy}.ts`, `packages/llm/llm-deepseek/src/{adapter,index}.ts`, service/adapter tests | `PreparedProviderCall`, `RetryPolicy`, `DeepSeekModelConfig`, exact-model resolution | Oracle `prepare`; `tests/provider_contract.rs` | `compatible` | Exact model context/output defaults, unlisted-model fallback, adapter-default markers, default/custom retry facts, and instance-bound one-shot preparation are frozen before logging. Phase 3 will execute retries; Phase 2 never retries inside one provider stream. |
| DeepSeek request serialization and thinking controls | `packages/llm/llm-deepseek/src/{serialize,types}.ts`, `tests/serialize.spec.ts` | `src/provider/deepseek/request.rs` | Oracle `serialize`; `request_serialization_*` tests | `compatible` | Scoped to accepted provider-neutral requests. DeepSeek wire structs remain private. Image input and unsupported effort fail before HTTP exactly as the compared cases require. |
| Post-v0.1 workspace image reading and DeepSeek vision | Fixed `packages/fs/tool-fs/src/read-image.ts`, `packages/attachment/attachment{,-local}/`; latest master `packages/llm/llm-deepseek/src/{index,serialize,adapter}.ts` at `cd5ef8148158c3a752a658978873241fdf8e2bbc` | `src/{attachment,tools/read_image}.rs`, model-route tool binding, DeepSeek request integration, and CLI assembly | `upstream_phase54_image_read.json`; four-format/store/resume/tool/provider tests plus real two-request `cli_smoke` journey | `intentional-difference` | Fixed upstream's DeepSeek adapter rejects images; latest master adds the exact visual route, Files API, base64 fallback, and request-image normalization. Rust combines the fixed tool/store contract with latest ordered inline behavior, but uses a Session-root store, base64 original rasters only, exclusive image-tool scheduling, and stricter four-image/4 MiB request bounds. This increases repeated request bytes and rejects larger batches while preserving logged references and model-visible order. |
| DeepSeek SSE framing and response translation | `packages/llm/llm-deepseek/src/{sse,translate}.ts`, corresponding tests | `src/provider/deepseek/{sse,response}.rs` | Oracle `sse`/`translate`; arbitrary byte-split and translation tests | `compatible` | Covers BOM, UTF-8 and CR/LF splits, comments, multi-data, strict blank-line dispatch, `[DONE]`, text/reasoning/interleaved tools, usage, finish, malformed JSON, empty completion, and truncated streams for inputs inside Rust resource bounds. |
| Provider-neutral whole-stream grammar | `packages/llm/llm/src/invariant.ts`, `tests/invariant.spec.ts` | `src/provider/stream.rs` | Oracle `wholeStreamInvariant`; `tests/provider_contract.rs` | `compatible` | Scoped to canonical streams whose block index is not reused after close. Block/delta/end types, usage uniqueness, terminal finish, open-block error semantics, and post-finish rejection match. |
| Closed provider block-index reuse | Upstream invariant accepts close-then-reopen, while `BlockAssembler` ignores the reopened block | Rust `StreamValidator` rejects any reused index | Oracle `closedIndexMayBeReused`; `stream_validator_rejects_closed_index_reuse_as_a_documented_rust_difference` | `intentional-difference` | The two official upstream layers disagree; DeepSeek translation itself uses monotonically allocated indices. Rust rejects the ambiguous stream so no consumer can see a validator-approved block that assembly later discards. A custom provider relying on reuse must allocate a new index. |
| DeepSeek HTTP/auth/cancellation/error facts | `packages/llm/llm-deepseek/src/{adapter,index}.ts`, `packages/llm/llm/src/{api-key,error,adapter-failure}.ts`, adapter/service tests | `src/provider/deepseek/{adapter,credentials,error,transport}.rs` | Fake-transport lifecycle matrix and real loopback success/302 tests | `compatible` | Scoped to one request with no retry: per-call credential lookup, one POST, lazy body reads/backpressure, cancellation, idle timeout/comment activity, consumer drop, stable failure codes, Retry-After, request-ID precedence, connection loss, missing body, and one terminal finish. Retry execution is Phase 3. |
| Provider security, attribution, and resource policy | Upstream accepts trusted launch-environment endpoints, follows its own product attribution, sends an anonymous ID, retains provider error text, and has no equivalent fixed in-memory limits | Trusted process/caller endpoint only; HTTPS or loopback HTTP; no redirects/system proxy/anonymous ID; independent User-Agent; bounded, control-cleaned, secret-scrubbed requests/responses | Endpoint/redirect/proxy/secret/Debug/resource boundary tests in `provider_contract`, `deepseek_loopback`, and provider unit tests | `intentional-difference` | Rust prevents a workspace file, process proxy, or redirect from choosing where a key is sent, avoids claiming official identity, and fails closed on oversized or unsafe values. This can reject large streams/configs and custom workspace/proxy gateways that official Harness accepts; users must explicitly configure a trusted HTTPS endpoint. Error messages are safe summaries rather than byte-identical provider text. The tests prove no proxy/redirect, no secret in diagnostics, and every enforced rejection. |
| DeepSeek configuration lifecycle | Cordis settings can hot-reload a route and keep the last valid snapshot; old prepared calls retain their registration | One immutable `DeepSeekProvider` per validated connection snapshot; create a replacement instance to change it; credentials still resolve per call | Binding/config snapshot tests; `prepared_call_is_bound_to_the_exact_provider_instance` | `intentional-difference` | Rust has no Cordis/HMR registry. Immutability prevents mixing endpoint, key reference, model facts, and retry policy across generations, but embedders must replace the provider explicitly. An old prepared request is rejected by another instance instead of following the replacement route. |
| Malformed/oversized DeepSeek config and live wire values | JavaScript layers admit some values such as explicit `maxTokens:0` or large positive catalog integers farther into preparation, and the in-memory adapter has no fixed count/byte budgets | Positive safe token/catalog values and bounded model/retry metadata; malformed typed wire values and unknown live chunks fail closed | Public prepare/config tests, wire rejection tests, and resource-limit tests | `intentional-difference` | Rust refuses invalid or resource-hostile facts before network/session publication. A custom caller using such loose values must correct them; accepted canonical DeepSeek requests and events are unchanged. This makes later Phase 3 retry and token-limit decisions typed and bounded. |
| Request-header change detection | `packages/llm/llm/src/call-config.ts`, `packages/core/session/src/request-header.ts`, request reconstruction/resume tests | `EpochHeader::equivalent_to` and the Agent producer write `initial`, `resume`, or `change` full snapshots before dispatch | Oracle `callConfigEquality` plus `requestHeaderLifecycle`; `request_headers_are_suppressed_when_stable_and_marked_on_resume_or_change` compares the generated full payloads | `compatible` | Both runtimes use the same system/config facts and three turns whose effective `maxTokens` are `1024 → 1024 → 2048`. The comparison fixes the exact canonical `initial`/`change` payload pair, stable suppression, and the exact `initial`/`resume` payload pair from a fresh loop over the first loop's complete event seed. Rust uses explicit field-wise equality rather than JavaScript object-string byte order, as recorded above. |
| Post-v0.1 Session model and reasoning-effort selection | Fixed Agent model-selection, Host `models`/`selectModel`, DeepSeek adapter and Web selector sources/tests recorded in `docs/upstream.md`; latest Session Controller behavior rechecked | Idle `/model [MODEL [EFFORT]]` reports the effective selection or atomically replaces the next DeepSeek call after side-effect-free Provider validation; unlisted ids pass through, omitted effort resolves to the Provider default, invalid input preserves the prior route, and only a later `request/header` makes the choice durable | Source-attributed Phase 52 fixture; parser/palette/Agent request-order and failure tests; enhanced select → exact wire/header plus zero-ANSI linear durable-resume PTY journey; local repository gates; `docs/design/session-model-selection.md`; `docs/validation/phase-52.md` | `partial` | Rust has one idle text command and one DeepSeek route instead of a running-step-safe Web RPC, dynamic multi-Provider catalog, popup, image-capability admission or saved deployment default. Token-shaped model ids are capped at 256 bytes. An unconsumed choice is lost on exit, as upstream Session durability also begins at the consuming request header. No executable cross-language selector producer exists. |
| Post-v0.1 durable safe permission presets | Fixed permission-preset service, tests and client contract recorded in `docs/upstream.md`; latest-master retention rechecked | Idle `/permission [ask|auto-edit]`, closed log-only `permission/preset`, last-value resume/fork and explicit-startup override; `auto-edit` allows prepared file tools while Shell and plugins remain Ask | Source-attributed Phase 53 fixture; codec/projection/Agent/parser/palette tests; enhanced policy split, startup/resume/override and zero-ANSI linear PTY journeys; local repository gates; `docs/design/permission-presets.md`; `docs/validation/phase-53.md` | `intentional-difference` | Rust cannot offer official `workspace-write`/`danger-full-access` bundles because it has no proven OS sandbox. It persists one split file-policy preset rather than emitting sandbox and whole-approval knob events, and omits deployment settings, custom bundles and the Web picker. |
| Agent canonical turn/step/request loop | `packages/core/agent-loop/src/{agent,invariant}.ts`, session request/surface folds, loop/request-reconstruction tests | `src/agent/mod.rs` owns bounded turns, steps, attempts, provider dispatch, stream assembly, and truthful closure | Generated Phase 3 oracle plus `rust_core_traces_match_the_committed_upstream_phase3_oracle` | `compatible` | Deliberately scoped to the five compared core scenarios: text completion, one tool round trip, one same-step retry, max-token tool suppression, and pre-step rejection. The test derives Rust input chunks from the official fixture and compares dispatch-time replay, full requests, core event order, chunks, surface provenance, final messages, counters, and closure. Inbox/Cordis behavior and safety policies are separate rows below. |
| Agent inbox, pre-step, prompt ownership, and pre-cancel entry | Upstream owns an inbox, writes `agent/inbox/spliced`, runs Cordis pre-step/runtime-context waterfalls, and rejects an already-cancelled call before `turn/start` | Phase 3 takes an explicit `TurnProposal`, fixed system text, and ordered schemas; the Phase 7 CLI now creates each proposal from one admitted terminal/script record and writes a balanced aborted turn for an admitted pre-cancelled call | Generated oracle records the two inbox events per canonical case; comparator removes exactly that type; zero-step/pre-cancel/input-boundary and real multi-turn CLI tests | `intentional-difference` | The smaller Rust API deliberately has no Cordis/plugin inbox. Event traces therefore omit inbox/runtime-context facts, Queue/Steer remains unavailable, and pre-cancel produces two Rust boundary events where upstream can produce none. Phase 7 owns terminal input without silently changing this Phase 3 seam: it admits at most one prompt and passes it through the same explicit proposal API. |
| Normal same-step retry and durable retry events | `packages/llm/llm-retry/src/{index,types,invariant,history}.ts`, retry/transport-recovery tests | `src/agent/retry.rs` and session `llm/retry`/`llm/retry-started` events | Generated `retrySameStep` trace; Agent retry, Retry-After, cancellation, policy-key, and replay tests | `compatible` | Scoped to an installed upstream retry plugin with canonical normal policy. The comparison fixes the failed chunk, policy key, failure, delay, retry identity/number, same-step second dispatch, final projection, and event order. |
| Retry ownership and Agent safety limits | Upstream retry is a removable Cordis plugin; `always` may retry indefinitely, and the in-memory Agent has no total step/attempt/tool/token/time budget | Rust always executes the prepared policy inside `AgentLoop`; every mode is bounded by validated attempt/retry/step/tool/token/time/event/byte limits | `agent_limit_builders_enforce_every_public_hard_ceiling`, retry/attempt/token/timeout/event-floor tests | `intentional-difference` | One owned state machine is easier to audit and prevents a CLI from looping or growing memory forever. Long but otherwise valid upstream turns can stop earlier with a stable `AGENT_*` error, and embedders cannot dispose retry execution independently; accepted canonical short traces are unchanged. |
| Canonical single-tool round trip | `packages/core/agent-loop/src/tool-calls.ts`, Agent tool-order/call tests, `packages/core/tools/src/index.ts` | Serial `ToolExecutor` seam with intention-before-body, result provenance, and next-step replay | Generated `singleToolRoundTrip` trace plus the Phase 3 oracle comparator | `compatible` | Scoped to one declared, valid, successful tool call. The comparison fixes the exact name/arguments/call ID, proves `tool/call` is committed before the body starts, checks result provenance, and compares the next provider request. The real registry/policy/approval pipeline begins in Phases 4–5. |
| Tool scheduling, malformed input, cancellation cleanup, and output bounds | Upstream runs explicitly parallel-safe tools in a rolling pool, passes malformed non-empty JSON onward, drains started work without a fixed grace deadline, and has no generic result-byte cap | Rust now overlaps only the Phase 30 read/Web whitelist; every other tool remains an exclusive barrier. It still rejects duplicate IDs, turns invalid/oversized arguments into model-facing results, bounds preferred results, and gives cooperative cleanup one second before dropping a non-responsive future | Phase 30 rolling-cap/order/barrier/cancellation/failure tests plus the existing bad-argument, duplicate, budget, cleanup, timeout, and cancellation matrix | `intentional-difference` | Shipped safe reads regain bounded concurrency without weakening hard bounds. Rust still rejects malformed calls instead of dispatching them, may truncate oversized results with `TOOL_OUTPUT_BUDGET_EXCEEDED`, and stops waiting after the cleanup grace. Mandatory small correlation fallbacks remain protected by the Session reservation. |
| Post-v0.1 bounded parallel-safe tool scheduler | Fixed Agent Loop scheduler/runtime/read/Web sources and deterministic tests recorded in `docs/upstream.md`; latest master retains the scheduler | Production rolling pool with default cap ten, exact `read`/`web_search`/`web_fetch` whitelist, exclusive barriers, model-order results/context, cancellation drain, and existing Session/timeout/resource ownership | `parallel_safe_tools_use_a_rolling_cap_and_commit_results_in_model_order`; exclusive-barrier, cancellation, infrastructure-drain, registry-whitelist, and real CLI two-request barrier tests; `docs/validation/phase-30.md` | `partial` | Observable shipped behavior is locally covered. Rust intentionally uses an immutable name-based classifier, fixed-per-Agent cap, one-second cleanup grace, and existing durable unknown-outcome repair; it has no dynamic Settings layer or generated cross-language scheduler oracle. |
| Post-v0.1 advisory repeated-tool reminder | Fixed default bundle, repeat-tool package/source/tests, Agent next-step inbox, and ordered tool finalization recorded in `docs/upstream.md`; latest master retains the behavior | Per-`AgentLoop` exact call chain, official 3/5/8 texts, 500-scalar preview, direct-human reset, Goal-style continuation, definite-result observation, and ordinary source-attributed next-step Session message | Fixed-source fixture comparator; canonicalization/reset/error/parallel tests; real CLI three-read journey; `docs/validation/phase-31.md` | `partial` | Rust ships only the fixed defaults, with no wildcard configuration or Cordis hook surface. It stages rather than durably inbox-logs an undispatched notice, starts fresh on reconstruction, and uses bounded JSON plus Unicode-scalar preview; no executable cross-language producer exists. |
| Executor infrastructure failure and unresolved tool history | Upstream keeps the assistant tool call and started `tool/call` without inventing a result, closes the turn, and may accept a later follow-up whose real provider can reject the dangling history | Rust preserves that same append-only prefix, then returns `Poisoned` on the same instance and `UnresolvedToolCall` after reconstruction until append-only repair exists | Infrastructure/panic closure tests plus same-instance, reconstruction, and spoof-resistant surface-provenance guard tests | `intentional-difference` | Refusing more model/tool side effects is safer than sending protocol-invalid history. A session with this rare infrastructure tail cannot continue in Rust Phase 3; Phase 8 will append a recovery result without rewriting old evidence. Ordinary model-facing tool failures still have results and remain reusable. |
| Trusted in-process Agent extension failures | Upstream TypeScript extensions can throw through framework seams and process diagnostics follow the host runtime | Rust catches provider/tool factory and poll panics, cancels children, closes with fixed durable errors, and never stores the panic payload | `provider_and_tool_panics_close_without_persisting_the_panic_payload` | `intentional-difference` | This protects the session from opaque extension text, but the global Rust panic hook runs before `catch_unwind`, so stderr redaction and hostile native-code isolation are not claimed. Provider/tool traits are trusted and must return promptly, cooperate with cancellation, avoid detached work, and return only authorized durable content. |
| Read-only registry ownership and durable pipeline | Upstream `ToolRuntime` is Cordis-extensible and Agent Loop owns call/result publication | Immutable `ReadOnlyToolRegistry` implements `ToolExecutor`; one canonical assembly uses its schema snapshot and executor together, while Agent still owns durable publication | `schemas_have_stable_order_and_closed_parameter_objects`; real-registry Agent trace checks assistant → call → result → step, provenance, and next-request replay; the Phase 3 tool-body prefix test plus the registry's lazy future fix intention-before-body | `intentional-difference` | Official tools can be extended through framework hooks; Rust exposes a closed four-name catalogue so the filesystem authority and schema set are auditable. Users cannot dynamically register Cordis tools; the combined Phase 5 registry adds the separately documented approval/write path. Combined generic timing and real-registry tests prove that filesystem work cannot begin before the durable intention. |
| Post-v0.1 DeepSeek-backed `web_search` | Fixed `packages/web/{tool-web,web,web-search-deepseek}` production/tests and shipped search-only composition recorded in `docs/upstream.md`; latest master multi-query/trust/fetch changes inspected separately | Production CLI accepts one to four strict queries, concurrently sends one bounded native DeepSeek search request per exact unique query, fail-fast cancels/drains siblings, fairly merges/deduplicates at most eight sources, labels external text untrusted, needs no approval, and records the ordinary call/result order | Parser/renderer/merge/concurrency/provider endpoint/auth/cancellation/timeout/body-limit unit tests; registry fake-provider test; real script-mode two-query loopback journey with exact search wire shapes, next-request result, durable call-before-result order, and no approval; `docs/design/{web-search,web-fetch}.md`; `docs/validation/phase-{28,29}.md` | `partial` | Phase 28 first implemented the fixed one-string schema; Phase 29 intentionally moves the model surface to inspected current master's one-to-four `queries`. Rust keeps additional byte/resource caps and omits the fixed provider's dedicated `web/deepseek-search-llm-request` event and specialized Web card. No generated cross-language oracle exists. |
| Post-v0.1 current-master multi-query search and public `web_fetch` | Fixed unsafe fetch and latest hardened `packages/web/{tool-web,web-fetch-http}` production/tests plus latest standard-preset composition recorded in `docs/upstream.md` | Anonymous HTTP(S) provider with closed URL schema, complete DNS-answer public policy, IPv4/IPv6/DNS64 checks, address-pinned no-proxy transport, per-hop same-origin revalidation, deadline/cancellation, byte/character/output caps, supported text decoding, conservative HTML conversion, untrusted notice, no approval, and ordinary durable call/result facts | Query concurrency/failure/merge tests; URL/IP/DNS64/pinning/type/charset/size/timeout/cancellation/HTML/render tests; fake-provider registry path; real two-query search and loopback-blocked fetch CLI journeys; complete local all-target gates; `docs/design/web-fetch.md`; `docs/validation/phase-29.md` | `partial` | This is an inspected post-baseline extension, not a fixed-commit compatibility claim. Rust intentionally uses conservative HTML conversion, fewer charset labels, ordinary tool facts, generic terminal cards, and serial Agent-level tool scheduling. No live public fetch or generated cross-language oracle was used. |
| Canonical workspace-internal `read` page content | `packages/fs/tool-fs/src/{read,read-target,read-render}.ts` and read tests | Bounded UTF-8 regular-file scanner with upstream-shaped numbering, pagination, empty-file and missing-file behavior | Type-checked Phase 4 oracle `canonical.read`; `canonical_results_and_workspace_denials_match_the_phase4_oracle_scope` | `compatible` | Narrowly scoped to the committed small full/window/empty inputs and missing-file code after normalizing the displayed path. Resource admission, NUL policy, path display, and structured presentation are separate rows. |
| `read` path display and structured result | Upstream text displays an absolute path and success carries structured `value` plus presentation `meta` | Rust deliberately displays a workspace-relative path and persists one authoritative text block plus optional failure, with no success `meta` | The oracle retains upstream content/value/meta; the comparator explicitly removes only `<workspace>/`; result tests assert Rust `meta` is absent, replay is exact, and absolute workspace text does not leak | `intentional-difference` | A local terminal agent benefits from not publishing its host root and from one model/session/UI result instead of a second presentation-only file object. Upstream consumers expecting `value`/`meta` cannot consume Rust results byte-for-byte, but model-visible content, append-only replay, and the CLI remain deterministic; no session event is hidden. The five-part rationale is recorded in `docs/design/read-only-tools.md`. |
| Canonical workspace-internal `glob` and `grep` content | `packages/fs/tool-fs-search/src/{glob,grep,search-core,presentation}.ts` and search tests | Capability-native traversal with globset/byte-regex matching and deterministic rendering | Type-checked Phase 4 oracle `canonical.glob`/`canonical.grep`; exact match/no-match comparisons | `compatible` | Scoped only to the committed small canonical fixtures. Large-result policy, equal-mtime tie-breaking, ignore/hidden/binary edge cases, and structured value/meta are not included in this compatible claim. |
| Read-side confinement, strict arguments, traversal, and resource policy | Upstream local reads/search accept absolute paths, `..`, and followed external symlinks; input roots allow extras; in-memory tools use different/looser limits | One startup capability; parent/sibling/external-symlink and symlink-directory traversal rejection; closed typed args; regular-file checks; bounded scans and compact-JSON output | Paired oracle ambient read/glob/grep acceptance versus Rust denial; `read_only_tools_security`; strict/null/schema tests; parameterized and representative exact/one-over resource tests | `intentional-difference` | Rust protects a terminal user's workspace and memory: it rejects outside reads, special files, malformed/extra/null fields, and oversized work that official Harness may accept. This can reject otherwise valid upstream calls, including multibyte strings whose character count fits JSON Schema but whose UTF-8 bytes exceed 4,096. It is an in-process capability boundary, not an OS sandbox, filesystem snapshot, or hard timeout for a stuck kernel call. |
| Model-facing one-level `list` | Upstream exposes only internal `FileSystem.listDir`; its tool catalogue explicitly has no model-facing list | Bounded `list {path?}` with byte-stable ordering, file/directory/symlink/other markers, count, and truncation | Oracle `listDirPrimitive` proves the upstream primitive/tool absence; Rust schema, ordering, truncation, path, special-file, and real-Agent tests | `intentional-difference` | Rust adds a useful model capability rather than mislabeling the internal service as an official tool. Models see one extra schema and Rust-specific text/type markers; upstream follows an internal file symlink for classification whereas Rust reports the link itself. The tool remains read-only and workspace-confined. |
| Normalized file-change effects and approval audit order | `packages/fs/tool-fs/src/{write,edit,diff}.ts`, `packages/fs/fs-local/src/{index,fsio}.ts`, `packages/core/tools/src/index.ts`, `packages/core/agent-loop/src/tool-calls.ts`, and `packages/interaction/user-approval/src/{index,invariant}.ts` plus their focused tests | Public `WorkspaceToolRegistry` prepares and commits one mutation through `AgentLoop`; Session owns paired log-only approval events and correlated results | Type-checked Phase 5 oracle; `canonical_file_changes_match_the_committed_phase5_oracle_scope`; `approval_order_and_side_effects_match_the_committed_phase5_oracle_scope` | `compatible` | Deliberately narrow normalization: the four committed create/update/edit cases compare exact final UTF-8 bytes, operation class, and update hunk facts; the four decision cases compare first-step assistant/call/optional approval/result/step order, decision/result state, call provenance, answerer and model-dispatch counts, and side effects. Upstream `defaultAllow` maps to explicit Rust `Allow`. This row does not claim identical tool names, arguments, result envelopes, path text, create-preview metadata, default policy, conflict windows, or resource admission. |
| Legacy `apply_patch` surface, exact preview, default approval, observation, and write safety | Upstream exposes `write` and literal `edit`, normally allows workspace writes without asking, keeps process-local read observations, follows broader path/link behavior, and can overwrite an external change in the final check-to-rename window | Unix-only strict single-file `apply_patch` beside the official-name tools; side-effect-free canonical preview; default `Ask`; fail-closed provider; capability-confined no-symlink/no-hardlink publication; fixed limits; late conflict rechecks; committed/not-committed result facts | Phase 5 oracle `toolSurface`, `canonicalMutations`, `observationFailures`, `windowedObservation`, `lastWindowOverwrite`, and `approvalPipeline`; the two oracle comparators; `default_rust_file_policy_asks_and_fails_closed_without_a_ui`; patch, rejection/conflict, path/link, resource, cancellation, timeout, and replay tests | `intentional-difference` | Upstream has no `apply_patch` schema or applied-diff approval request; Rust keeps one extra closed patch schema and asks with the complete canonical diff, then stores relative `path`, one `diff`, and `committed` rather than upstream absolute-path text, execution `before`/`after`, and `meta.diffs`. Rust treats exact patch context plus preparation-time baseline as its observation instead of requiring a prior model-facing `read`, so stale codes and timing differ. Ask-by-default prevents writes when no UI exists; stricter paths and limits can reject upstream-valid operations. Late checks narrow but cannot eliminate the portable final rename race, so Rust does not claim cross-process linearizability. Windows mutation support is not claimed. |
| Post-v0.1 fixed `str_replace_editor` | Fixed `packages/fs/tool-str-replace-editor/{src/index.ts,tests/tools.spec.ts,README.md}` and base/minimal preset registration recorded in `docs/upstream.md` | Closed official-name `view`/`create`/`str_replace`/`insert` schema over the retained workspace; numbered/ranged file view, filtered two-level directory view, empty creation, one unique literal replacement, zero-based insertion, and existing prepared-file approval/publication path | Source-attributed Phase 32 fixture; schema/parser/clipping unit tests; all-command Agent fixture comparison; ambiguity/relative/denial/conflict/empty/tree tests; ask and auto-edit real PTY journeys; `docs/design/str-replace-editor.md`; `docs/validation/phase-32.md` | `partial` | Canonical small outputs and edit semantics match the fixed source fixture, but Rust keeps stricter workspace/link/resource and line-ending rules, default Ask plus explicit auto-edit, atomic publication, and no Cordis observation-policy lifecycle. Empty/no-text-difference approval uses a truthful opaque preview because unified diff has no material hunk. JavaScript UTF-16 clipping and every backend error string are outside the narrow comparison; no generated cross-language producer exists. |
| Explicit interactive auto-edit policy | Upstream tool listeners can pre-decide `allow`, `deny`, or `ask`; ordinary workspace writes commonly allow, while the separate approval-session policy is only `ask` or `never` | Interactive `--approval-mode ask|auto-edit` plus idle `/permission`; `auto-edit` maps only fully prepared built-in `write`, `edit`, `apply_patch`, and `str_replace_editor` mutations to `Allow`, while Shell/plugins remain `Ask` and every script action remains `Deny`; the narrow preset is Session-durable | Closed parser/help/list/script-admission tests, including stdin-PTY plus separately redirected stdout/stderr before plugin loading; policy-isolation and permission-event tests; real PTY auto-edit, runtime switch, Shell isolation, resume and explicit-override journeys; existing path/link/conflict/cancellation/resource suites; local repository gates; `docs/design/{approval-modes,permission-presets,str-replace-editor,write-edit-tools}.md`; `docs/validation/{phase-11,phase-32,phase-34,phase-53}.md` | `intentional-difference` | This is not an auto-answer, unrestricted mode or sandbox. It changes no path, link, conflict, cancellation, resource, intent-before-side-effect, or replay check. The selection is explicit, interactive-only and may still create or substantially rewrite one regular workspace file; unlike the exact-Shell grant, its narrow file policy now survives Session resume. |
| Exact built-in Shell process grant | Pinned user-approval and ACP paths expose only one-shot allow/reject/cancel/unavailable answers; approval-session policy is `ask` or `never`, and unknown ACP answers fail closed | Explicit terminal-only `Allow exact Shell for this process`; Agent-owned 64-entry process-keyed HMAC cache over sealed execution identity including foreground/background mode; consume before use and reinsert only after clean foreground exit or successful background ownership handoff plus authoritative result commit; no Session restoration or plugin/script/file use | Selector/Dock/request-scope and fresh-confirmation tests; digest capacity, entropy, redaction, identity-field/mode, and allocation-fallback tests; exact event-order, ordinary-once, nonzero, failed-hit, cancelled-hit, and Agent-rebuild tests; enhanced and linear repeated-Shell PTY journeys; same-process Shell-grant-then-plugin isolation PTY; full repository gates; `docs/design/{approval-modes,background-shell-jobs}.md`; `docs/validation/{phase-11,phase-42}.md` | `intentional-difference` | This is a Rust terminal-product difference, not an upstream `allowed-once` reinterpretation. The first prompt persists the ordinary `allowed-once`; a hit has no fabricated approval pair. Exact repetition remains arbitrary unsandboxed native code and may repeat destructive, detached or network side effects. |
| Canonical small foreground `bash` result rendering and exit facts | `packages/shell/tool-bash`, `packages/shell/bash-local`, `packages/shell/shell`, and subprocess sources/tests recorded in `docs/upstream.md` | `LocalToolRegistry` prepares one approval-gated foreground Bash Action; the owned runner returns a bounded correlated result through `AgentLoop` | Type-checked Phase 6 oracle plus `canonical_foreground_results_match_the_committed_phase6_oracle` | `compatible` | Deliberately limited to the five committed small foreground cases—success, silence, mixed stdout/stderr, nonzero exit, and real self-signal—at the same explicit 25-second timeout. The comparison checks exact rendered text and exit code/signal/`timedOut`/timeout facts. Schema/default policy, workdir, environment, output limits, cancellation, and quiescence are excluded and tested in the next rows; no absolute-path, spill, sandbox, background-job, or extension-metadata compatibility is claimed. |
| Shell authority, approval, workdir, environment, executable, and output policy | Upstream normally permits ordinary shell calls, accepts broader workdirs, resolves `bash` through `PATH`, inherits a scrubbed ambient environment including ordinary startup hooks, supports spill files/background jobs, and may add sandbox escalation | Separate macOS/Linux `LocalToolRegistry`; one retained Workspace capability; default `Ask`; direct-execute guard; capability-held initial CWD; fixed `/bin/bash --noprofile --norc`; fixed environment allowlist; bounded in-memory tails plus private spill/compact result; foreground and process-local background modes; no sandbox | Oracle schema/workdir/environment/startup facts; `local_registry_exposes_background_bash_and_closed_job_schemas`, policy/direct-gate/workdir/shared-authority/environment/output/spill/background tests | `intentional-difference` | Ask-by-default and the closed schema prevent an unassembled Agent from silently running code. Rust rejects absolute/outside/symlink workdirs, ignores PATH wrappers and `BASH_ENV`, removes ambient secrets/proxies/`DSH_*`, stops after 8 MiB combined observed output, and bounds encoded results to 64 KiB. Background mode keeps the 295-second timeout and finite registry rather than upstream's unbounded-duration producer. These rules can reject or change valid upstream commands. The retained workdir only controls process startup: an approved command is arbitrary unsandboxed code and may later access anything allowed to the user. A Tokio I/O driver is required and checked before spawn. macOS and Ubuntu 24.04 are accepted; Windows is not implemented or claimed. |
| Post-v0.1 foreground Shell spill files | Fixed subprocess collector, Bash executor/renderer, tests, and spill architecture note recorded in `docs/upstream.md`; latest master retention confirmed | Lazy per-stream capture keeps the 64,000-byte tail, creates one random 0700 directory and exclusive 0600 files on overflow, flushes before publishing paths, and projects bounded locators/counts through Session and TUI | Source-attributed Phase 33 fixture; spill write/flush, uneven-overflow, process cancellation/output-limit/dual-stream, renderer/legacy-projection, real Agent, and real CLI tests; local repository gates; `docs/design/shell-output-spill.md`; `docs/validation/phase-33.md` | `partial` | The normal foreground overflow path matches the official tail plus full-output notice, but no generated cross-language fixture proves full compatibility. Rust preserves only bytes observed before its stricter 8 MiB aggregate stop, labels that forced-stop file as captured rather than full, uses approved Bash rather than workspace `read` for retrieval, and keeps no generic cross-tool spill policy. Temporary artifacts are owner-only convenience files, not durable Session storage. |
| Post-v0.1 bounded background Shell jobs | Fixed jobs service/local registry, `tool-jobs`, Bash producer/background adapter and focused unit/integration tests recorded in `docs/upstream.md`; latest master lifecycle, completion delivery and cursor retention confirmed | Approval-gated `run_in_background`; process-local `bash-N` ownership; closed `job_list`, consuming incremental/final `job_output` with wait, and `job_kill`; independent bounded stdout/stderr offsets with explicit loss/spill reporting; eight active/64 retained jobs; exact `tool-jobs` completion notices; busy next-step injection; three human-reset idle wakes; terminal-read/kill/teardown suppression; shutdown cancel-and-join | Source-attributed Phase 42/43/44 fixtures; schema/parser/approval/exact-mode/background completion/live and terminal cursor/wait/kill/process-group cleanup/notice content/inbox/wake-budget/suppression/foreground regression tests; real Agent and terminal background journeys; local repository gates; `docs/design/{background-shell-jobs,background-job-notices,background-job-incremental-output}.md`; `docs/validation/phase-{42,43,44}.md` | `partial` | Rust supports only Bash jobs in one CLI process and keeps its 295-second command cap. Each read consumes newly observed bytes; falling behind keeps a fixed 64,000-byte tail per stream and live reads cannot reveal spill paths until final flush. Rust omits configurable quiet delivery/wake budget, multi-Agent owner fencing, final-output-only producers, terminal input/PTys and persistence. Its pending notice queue is capped at 64 concrete entries with an overflow fact. Old ids after resume are unknown and never replayed. |
| Post-v0.1 fixed-upstream `write` and `edit` tools | Fixed `packages/fs/tool-fs` write/edit sources, filesystem provider/policy, tests and code preset recorded in `docs/upstream.md`; latest master retention confirmed | Closed full-file create/overwrite and literal unique/explicit-replace-all schemas over the existing prepared workspace mutation owner; exact normal result wording, semantic diff approval, atomic commit, TUI projection and trusted workspace touch | Source-attributed Phase 34 fixture; parser/schema, create/update/empty/unique/all/no-match/ambiguity/expansion-limit/denial/rejection/cancellation/stale tests; real approved-edit PTY journey; local repository gates; `docs/design/write-edit-tools.md`; `docs/validation/phase-34.md` | `partial` | Normal small results and literal semantics match the fixed fixture, but no generated producer proves full compatibility. Rust keeps workspace confinement, complete approval diff, argument/session plus 16 MiB safe-text limits and late exact-baseline revalidation. Unlike the official default observation policy, a prior model `read` is recommended but not required because preparation plus human approval owns freshness. No image read, sandbox escalation field or unconstrained host filesystem is added. |
| Post-v0.1 project-local Skills catalog and loader | Fixed `packages/skill/{skill,skill-filesystem,tool-skill}` sources/tests and shipped code preset recorded in `docs/upstream.md`; latest master retention confirmed | One-level `.dsh/skills` then `.agents/skills` discovery through the retained workspace capability; bounded durable complete catalog; closed parallel-safe `skill {name}`; current-body reload; canonical resource/body frame; next-turn, next-step and resume replacement | Source-attributed Phase 35 fixture; scalar frontmatter/name/description/policy/priority/flat/bundle/symlink/file/root-limit/cancellation unit tests; closed success/error dispatcher test; real Agent write-refresh/load continuation; real enhanced-PTY load and resume replacement journeys; local repository gates; `docs/design/project-skills.md`; `docs/validation/phase-35.md` | `partial` | The useful fixed model-facing catalog and load flow is present, but no generated cross-language producer proves compatibility. Rust deliberately scans only the opened project, rejects symlinks, uses a strict common-scalar frontmatter subset and boundary rescans, and omits `$DSH_HOME`, `~/.agents`, custom/bundled/remote providers, watchers, arbitrary YAML metadata and direct `/name` injection. It never executes Skill scripts or grants permissions. |
| Post-v0.1 persisted `session_search` | Fixed `packages/session-query/{session-query,session-query-sqlite,tool-session-query}` extraction/filter/query/tool sources and tests recorded in `docs/upstream.md`; latest master retention confirmed | Always-available CLI `session_search` over normally closed current-version JSONL journals from the exact opened workspace identity; latest validated title or `untitled` across all five historical tools; literal phrase plus bounded Session id/creation/authorized-parent/root/availability and event sequence/time/type/surface filters; caller/busy exclusion; strict cold-scan reuse; 20 hits, 240-code-point snippets and per-session/aggregate/deadline bounds | Source-attributed Phase 36/41/46/47 fixtures; schema/query/extraction/filter/ranking/snippet/title/store workspace/caller/busy/corrupt/oversize/cancel/timeout tests; real filtered two-process five-tool journey with title assertion; local repository gates; `docs/design/{session-search,session-search-filters,session-search-titles,historical-tool-titles}.md`; `docs/validation/phase-{36,41,46,47}.md` | `partial` | Rust implements all five official tool names and public search-filter fields, but scans only unlocked persisted local journals, identifies workspaces by device/inode rather than exact `cwd` text, requires canonical ids and has no live source/cursor. A title inspection failure and a genuinely untitled log both render `untitled` without an official sanitized unavailable code. Occurrence/document/time ranking approximates rather than reproduces SQLite FTS5 rank; large/aggregate-overflow candidates are skipped with an incomplete-search notice, and no generated cross-language producer proves compatibility. |
| Post-v0.1 exact prior-Session event navigation | Fixed `packages/session-query/{session-query,session-query-sqlite,tool-session-query}` event-search/read sources and focused tests recorded in `docs/upstream.md`; latest master retention rechecked | Production required-id `session_event_search` and `session_event_read` over the same strict closed-journal workspace boundary; latest validated title or `untitled`; bounded ranked excerpts with sequence/time/type/surface filters, current/shadowed/log-only classification, exact target JSON and optional neighbor summaries; no approval or mutation | Source-attributed Phase 39/41/47 fixtures; schema/id/query/filter/ranking/surface/title/exact-window/output-limit tests; workspace/caller/busy/corrupt/oversize/not-found/cancel/timeout runtime tests; real filtered two-process search → event-search → exact-read → trace journey; local repository gates; `docs/design/{session-event-navigation,session-search-filters,historical-tool-titles}.md`; `docs/validation/phase-{39,41,47}.md` | `partial` | Rust excludes the current/live Session and requires a canonical explicit id; it defers cursors, live-preferred SQLite indexing and persistent indexes. Missing and unavailable titles both render `untitled`. Query tools remain Agent-serialized to cap local scan pressure. Exact responses that exceed the tool-result cap fail instead of truncating. |
| Post-v0.1 prior-Session relationship traces | Fixed `packages/session-query/session-query/src/{tracing,types}.ts`, tracing tests, and tool operations/presentation/workspace-access recorded in `docs/upstream.md`; latest master algorithm diff rechecked | Production required-id `session_trace` and `session_event_trace` over the strict normally closed same-workspace corpus; latest validated title or `untitled` for target/ancestors/descendants; deterministic ancestry, positional replacement chains and separate source/derived provenance | Source-attributed Phase 40/47 fixtures; schema/id/title/lineage/order/cycle/replacement/source/derived/surface/authorization/cancel/timeout tests; real two-process five-tool CLI journey; local repository gates; `docs/design/{session-tracing,historical-tool-titles}.md`; `docs/validation/phase-{40,47}.md` | `partial` | Rust does not trace the current/live Session or cross-workspace nodes. It requires an explicit canonical id, labels only persisted availability, renders absent/unavailable titles alike as `untitled`, shows unresolved ancestry as an opaque boundary and omits descendants outside its bounded strict corpus. |
| Post-v0.1 filtered Session retrieval | Fixed tool input/operations tests, provider-independent filters and SQLite filter-before-rank tests recorded in `docs/upstream.md`; latest master filter retention confirmed | Production official-name Session id/creation/authorized-parent/root/availability and event sequence/time/type/surface filters over the strict persisted-only corpus; title-enriched results; inclusive exact ISO bounds and filter-before-rank behavior | Source-attributed Phase 41/46/47 fixtures; closed schema, id/array/enum/range/offset/leap/sub-millisecond, AND/OR, parent authorization, filter/rank/title, cancellation and real CLI tests; local gates; `docs/design/{session-search-filters,session-search-titles,historical-tool-titles}.md`; `docs/validation/phase-{41,46,47}.md` | `partial` | Rust requires canonical ids, bounds arrays/strings, returns empty for live-only availability and retains explicit prior-Session targets. The upstream internal `cwd` clause is implicit in retained workspace identity. Cursors, live/current sources and persistent indexes remain absent; unavailable titles render like absent titles. |
| Post-v0.1 configured stdio `lsp` navigation | Fixed `packages/lsp/{lsp,lsp-stdio,tool-lsp}` service, framing, translation, host, process lifecycle, render sources and tests recorded in `docs/upstream.md`; latest master model contract and stdio lifecycle rechecked | Production CLI `--lsp-config` plus exact four-operation `lsp` tool, retained-workspace source access, lazy persistent configured server actors, bounded JSON-RPC/cancellation/process cleanup, and no server write authority | Source-attributed Phase 37 fixture; config/framing/capability/normalization/render/workspace unit tests; real CLI normal/crash/malformed/timeout/cancel journeys with deterministic stdio server and process-group cleanup; local repository gates; `docs/design/lsp-navigation.md`; `docs/validation/phase-37.md` | `partial` | Rust requires an explicit private versioned config and stable absolute non-symlink executable, uses one CLI workspace and stricter fixed resource/process bounds, and omits diagnostics, rename, symbols, call hierarchy, dynamic capability registration and arbitrary JSON-RPC. A deterministic Rust fixture exercises the real wire path, but no generated upstream comparison or real third-party server matrix exists. |
| Post-v0.1 durable per-step time context | Fixed `packages/context/time-context/src/{index,invariant,request-zone,timestamp}.ts`, focused tests, real headless smoke and durable time-context decision note recorded in `docs/upstream.md`; latest master decision-preservation change rechecked | Production CLI `--time-zone`; one bounded snapshot-form message per entered step; durable projection baselines across compaction/resume; canonical IANA/DST-aware formatting; stable pre-request failure and no request-header or authority change | Source-attributed Phase 38 fixture; zone/timestamp/duration/source/replay/compaction unit tests; two-step Agent order/failure/cancel tests; real CLI request, cold resume and pre-side-effect invalid-zone smokes; local repository gates; `docs/design/time-context.md`; `docs/validation/phase-38.md` | `partial` | Rust terminal configuration is explicit user-owned zone provenance and renders `Terminal time zone`; it does not reproduce browser RPC unique/mixed/missing derivation, ambient process fallback or positive refresh intervals. No generated upstream comparison proves broader compatibility. |
| Shell cancellation, timeout, and process-group ownership | Upstream detached POSIX children are new session/group leaders, start the command deadline immediately before subprocess spawn, use TERM→3-second→KILL, and separate direct foreground completion from later service-owned whole-group cleanup; caller abort discards the internal run result at the tool boundary | Sealed per-call Action; timeout starts after successful spawn; retained `WNOWAIT` leader; macOS `libproc` and Linux `/proc` observers; same-group TERM→KILL, pipe drain, exact reap, preferred started-result settlement, unresolved ownership-loss state, and process-local background-job ownership | Oracle caller-abort/direct-completion lifecycle facts; macOS/Linux process suites, private/public Agent Action tests, real Shell integrations and Phase 42/43 background shutdown tests | `intentional-difference` | Rust normally waits for two no-live same-group observations, pipe completion or bounded closure, and direct-child reap before publishing a started result; this is stronger and later than upstream foreground settlement. The first outer cancellation/turn-timeout reason remains stable while cleanup finishes, and a started result keeps bounded `started`/cleanup facts. If the wait status or observer contract is lost, Rust writes no fabricated result and poisons reuse. This does not prove an escaped `setsid`/changed-group descendant stopped; an escaped pipe holder only triggers bounded drain closure. Uninterruptible D-state or privilege-changing work can delay or prevent settlement. There is no sandbox or crash-time PID signalling; background ownership exists only in the current CLI and is never recovered. Linux namespace PID 1 and explicit child-subreaper hosts are rejected. macOS and Ubuntu 24.04 process paths passed their platform suites. |
| One-turn script output | `packages/bundle/headless/src/{startup,index}.ts`, ACP committed-message behavior, and the focused tests recorded in `docs/upstream.md` | `--prompt` and bounded piped-input drivers run the real Provider → Agent → Session path, wait for settlement, and print only the authoritative final assistant text | Committed Phase 7 oracle; `real_script_output_matches_the_committed_phase7_headless_oracle_scope`; real-binary `cli_smoke` suite | `compatible` | Narrowly scoped to the committed safe completed and non-completed cases: exact final-only stdout/LF and exit 0 versus 1. Rust visibly escapes terminal controls, treats input/usage and POSIX signals as separate stable exits, denies write/Shell approvals in script mode, and does not adopt upstream profile syntax, task-argument joining, JSONL flush/storage, or fixture-only `stream-json`; those differences are outside this narrow output claim. |
| Interactive terminal, approval, and turn cancellation | The fixed upstream has no built-in human TUI; ACP multi-turn/cancel/approval plus Web input/projection behavior are recorded in `docs/upstream.md` | A scrollback-first macOS/Linux terminal consumes post-commit Session projections, streams bounded text, joins one-shot approval, and keeps cancellation/resource cleanup owned | Phase 7 oracle comparators for two turns, cancel-then-continue, and allow/reject/cancel; the real-binary PTY suite covers styled/plain output, keyboard selection, stale input, signals, output failure, and terminal restoration | `intentional-difference` | Rust deliberately supplies its own terminal UX: local `/help`, `/exit`, and `/quit`, product-owned ANSI with a zero-ESC linear fallback, and a post-fence approval selector whose safe default is Reject. The Phase 7 linear path changes termios only for approval; the partial Phase 11 enhanced path instead owns long-lived cbreak and is scoped in its separate row below. Both retain `ISIG`, and tested decisions, cancellation, suspension/termination, EOF, and ordinary exits restore the exact original terminal state. Ctrl+C stops the current turn, Ctrl+D cleans up, and Ctrl+Z cleans up before suspension. This is intentionally not an external Turtle UI, Web UI, or ACP-wire implementation. |
| Narrow v0.1 local JSONL save, list, and resume | Pinned persistence coordinator/JSONL/checkpoint-policy/core-repair sources and tests recorded in `docs/upstream.md` | New CLI sessions use a bounded private JSONL writer; `--list-sessions` reads bounded headers; `--resume` validates current-version history and continues through the real Agent path. Damage or unsupported history fails before new Provider/tool work, and an unknown old tool outcome is never replayed | Durable Session/journal/path/store/recovery tests; real-binary list, script-resume, interactive-resume, corrupt/unsupported, and workspace-mismatch tests; `docs/validation/phase-8.md` | `intentional-difference` | Rust promises useful local continuity after a normal exit, not upstream-equivalent/database-grade power-loss durability. Existing locks, barriers, strict cold scan, and append-only repair are retained safeguards, but `SIGKILL`, power loss, disk failure, or filesystem failure may lose the final tail or make the session unresumable. The journal is convenience state, not a backup. |
| Post-v0.1 Session titles, manual rename, explicit refresh and fork increment | Fixed `session-title`, `session-title-llm`, `session-title-first-prompt-llm`, base bundle configuration and focused rename/refresh/persistence tests recorded in `docs/upstream.md`; latest master projection-registration-only diff inspected | First direct-human text creates a 5-word/40-byte control-cleaned fallback; an official non-loopback DeepSeek route records exact tool-free 64-token title requests and may append 80-byte provider replacements; idle `/rename <TITLE>` appends a pinned user title and cancellable `/refresh-title` retries the first-prompt provider or restores fallback-only title; latest title replays durably and closed journals expose it to list/resume and all historical query tools; `/fork` inherits the selected title facts and appends a bounded official-style user fork counter | Source-attributed Phase 45–49/51 fixtures; bounded first-prompt projection; normalization, event/projection/replay, Agent success/failure/cancellation/pinning/refresh, closed-journal scan, list, picker, command and query-title tests; local repository gates; `docs/design/{session-titles,session-search-titles,historical-tool-titles,manual-session-title-rename,manual-session-title-refresh,session-fork}.md`; `docs/validation/phase-{45,46,47,48,49,51}.md` | `partial` | Rust keeps a single Session writer, so completed background work is collected at a safe Agent boundary and explicit metadata commands serialize while joining superseded work. Loopback/test endpoints use deterministic fallback refresh. Busy, malformed or larger-than-16-MiB journals omit their title. Generic all-messages providers, overlapping/remote refresh callers, projection caching, per-title unavailable codes and a generated cross-language oracle remain absent. |
| Post-v0.1 current Session raw log export | Fixed Web `session-log-export` command plus Host archive source/tests recorded in `docs/upstream.md`; latest master Host ownership migration rechecked | Pathless idle `/export` flushes and copies the exact current durable JSONL prefix to a new owner-only workspace file in bounded cancellable chunks; failure/cancellation removes the unaccepted output and existing paths are never overwritten | Source-attributed Phase 50 fixture; journal/export-target/Agent unit tests; enhanced and linear real-PTY exact-byte journeys; repository-wide local gates; `docs/design/session-log-export.md`; `docs/validation/phase-50.md` | `partial` | Rust intentionally exports one current `.jsonl` instead of a ZIP because it has no product subagents or image-attachment store. It uses a generated workspace filename rather than browser download destination policy and omits generic durable command lifecycle facts. |
| Post-v0.1 completed-turn current Session fork | Fixed core SessionStore fork plus Host API fork source/tests recorded in `docs/upstream.md`; latest Session Controller ownership and client title-increment behavior rechecked | Idle `/fork [EVENT_SEQ]` copies the exact selected completed-turn prefix into a staged private journal, adds parent/seed metadata, a non-duplicate end-seed marker and bounded incremented title, then atomically publishes a separately resumable child without overwriting | Source-attributed Phase 51 fixture; boundary/copy/cancellation/target/title/Agent/recovery unit tests; enhanced parent-continues → child-resume and linear past-end real-PTY journeys; repository-wide local gates; `docs/design/session-fork.md`; `docs/validation/phase-51.md` | `partial` | Rust intentionally exposes a local terminal command for only the current live Session and leaves the parent open, printing a resume command instead of switching the current Agent automatically. It has no product-subagent Workspace attachment, external media copy or direct cold-Session fork. |
| Post-v0.1 broad upstream persistence, crash repair, and schema compatibility | The same pinned persistence sources plus their full crash/checkpoint compatibility behavior | Rust retains substantial tested substrate, but does not claim every `fsync` boundary, crash prefix, hostile warning-gate race, filesystem, or old/future schema migration | Phase 8 oracle and existing journal/recovery failure tests; exhaustive producer comparison pending | `partial` | This broader claim is explicit post-v0.1 hardening and is not advertised in README. |
| Narrow one-pass automatic context reduction | Pinned compaction-basic pressure/range/summarizer behavior and tool-pairing tests recorded in `docs/upstream.md` | At 80% committed pressure or one hard preflight failure, the real Agent prunes oversized tool results, attempts at most one tool-free 8,192-token summary in the turn, retains a balanced 16% tail, installs only a smaller checkpoint, and rebuilds the same pending input | Agent pressure/hard-limit success, Provider failure, active cancellation, timeout, invalid usage, tool-call rejection, memory/durable non-shrinking, marker-only and one-pass tests; real three-process `a_real_resumed_script_compacts_once_and_continues_the_same_prompt`; `docs/validation/phase-8.md` | `intentional-difference` | Rust intentionally permits only one summary request per turn and does not implement provider-overflow replay or upstream's repeated candidate loop. Pressure-only summary failure is advisory; hard-limit failure ends clearly. Summary-produced tools never execute. |
| Idle manual `/compact` | Pinned `command-compact`, compaction-basic manual path, region selection, and lifecycle types recorded in `docs/upstream.md`; latest master production command rechecked | Exact no-argument local command, below-pressure balanced selection, standalone null-turn compaction transaction, bounded provider request, strict smaller-checkpoint commit, cancellation/error closure, and no turn consumption | Session/Agent success, no-history, Provider-failure, cancellation, recovery, parser/palette tests, and real durable enhanced-PTY loopback journey; `docs/design/manual-compaction.md`; `docs/validation/phase-27.md` | `partial` | Rust correlates the transaction with `sourceCommandId` and adds its existing complete pre-request dispatch snapshot with trigger `manual`, preserving intent-before-model-call auditing. It intentionally omits upstream's generic `command/run`/`command/done` facts until all local commands share one designed durable command schema. Linear mode retains its established no-active-input behavior rather than rendering an in-turn busy notice. A generated cross-language oracle is absent, so this row is not `compatible`. |
| Post-v0.1 broad context compaction producer and physical ownership proof | Pinned compaction lifecycle/basic/pruner and token-meter sources/tests recorded in `docs/upstream.md` | Provider-neutral event/projection/recovery types, estimator/range selection, prune writer, attempt fold, and substantial resident-credit substrate exist, but provider-overflow replay, repeated transactions, cold ownership parity, complete closure headroom, and the shared 32/64/96/192 MiB proof are unfinished | Phase 8 oracle and existing compaction/recovery/resource suites; broad producer comparator pending | `partial` | Old events remain immutable and replacements affect only the model-visible surface. The cold scanner's 9 MiB row scratch is a separate bounded buffer, not shared-32-MiB accounting. These hardening gaps are not advertised as current product guarantees. |
| Phase 10 bounded subprocess tool plugins | Fixed upstream `ToolRuntime`, tool schema/pipeline docs and tests, CLI profile/npm loader, and subprocess service/local lifecycle sources recorded in `docs/upstream.md` | Explicit `--plugin-config` loader, bounded versioned NDJSON `hello`/`call`/`cancel`/`result`, host schema validation, the existing approval/Session order, and owned macOS/Linux process-group cleanup | Committed Phase 5 approval oracle compared with the real plugin Session order; strict protocol/config/schema/actor tests; installed text-stats/JSON-format journeys; crash, wrong-ID, invalid-output, cancellation, output-limit, startup-cancel, and no-replay fault tests | `intentional-difference` | Upstream plugins are trusted in-process Cordis/npm bundle layers and have no subprocess ABI. Rust deliberately uses a closed external-process tool-only transport: no hooks, Provider/Session replacement, npm loading, UI callbacks, hot reload, or sandbox claim. Invalid plugin arguments are rejected before Ask, unlike upstream `defineTool` validation inside execute. The transport is not wire-compatible; the compared call/optional-approval/result order is preserved. |
| Phase 11 semantic hybrid terminal UI | The pinned upstream has no built-in human TUI; its ACP and Web conversation seams provide committed partial/final display, approval takeover, and running-input queue/steer semantics. Its Web input-trigger core also fixes revision-stamped `/`/`@` spans, cancellable generations, and literal picks, but the shipped `@` source names running subagents rather than files. Current Claude Code terminal docs are a dated UX benchmark, not the DeepSeek semantic baseline | Bounded committed facts drive a production-reachable enhanced path with a grapheme-safe composer, next-turn FIFO, inline Dock, directional approval, final tool cards, joined receipts, Inspect/Review, six process-local semantic themes, a closed fifteen-command completion palette, bounded retained-authority workspace-file suggestions, process-local full/reduced working motion, a bounded header-only Session picker, and strict zero-ESC linear fallback. Assistant presentation recognizes fragment-independent headings, lists, quotes, inline/fenced code, fenced diff, and a source-preserving 2–8-column pipe-table subset. Tables keep exact source bytes and native wrapping, cap body rows at 64, physical rows at 16 KiB, and aggregate source at 64 KiB, and degrade unsupported/over-limit forms to plain text without changing Session facts. Canonical `apply_patch` preview styles still require generator provenance. The file picker scans only bounded relative paths, skips closed directories and symlinks, ranks deterministically, and inserts literal text without reading content or adding a Session/Provider fact. Bare interactive `--resume` lists at most 128 already validated headers for the retained workspace, shows only a safe basename, creation time, and short ID, and hands a confirmed ID to the unchanged recovery lifecycle. Full motion waits 300 ms and advances a separate ASCII phase cell at no more than 8 FPS; reduced motion uses only static one-/five-second text milestones. Emphasis, links, images, HTML, full resumed-history reconstruction, and real-emulator release evidence remain unfinished | Observer/projector/timeline/live/view tests; exact composer/decoder/queue/dock/screen, command/file/motion-state/read-fence/undo/request-isolation, palette and file presentation credentials, scanner/ranking/budget/cancellation races, paused-clock/generation/transaction-preemption, markup/table limit and fragmentation, canonical-patch provenance, Inspect/detail bounds, Debug-redaction, visible-control, 44/80/112 and compact-layout regressions; 88 real PTY journeys plus 2 PTY-harness tests covering themes, motion flag/runtime switching/process reset/linear zero-ESC, commands, file completion/failure/approval, Session picker nondefault select/context, stale and blocked-frame arming, cancel/empty/EOF/resize/suspend, Inspect/Review, active-stream resize, source uniqueness, semantic patch/table styling, signals, cards, receipts, and Session model selection/resume; a local installed-binary suite now covers queueing, Focus/Inspect/Review, receipts, read/search/patch/Shell, cancel/resume/compaction, and plugin approval, and its real PTY bytes generate the checked overview/approval/Review screenshots; the exact candidate passes the macOS/Ubuntu CI matrix; forced gated UI-panic cleanup, direct wide-fanout FD-peak measurement, emulator evidence, and another final review remain non-blocking hardening after the user-approved local completion boundary; `docs/design/tui-v2.md`; `docs/validation/phase-11.md` | `intentional-difference` | Rust supplies its own visual and key-binding system and claims no pixel compatibility with Claude Code, Turtle UI, or the upstream Web client. Queueing is a bounded local next-turn FIFO, not upstream same-turn steering. The implemented `@relative/path ` picker is literal text with no implicit file read and intentionally differs from upstream's subagent `@label `. The Session picker is workspace-filtered header discovery, not a history search: it does not show the last message or last-active time and does not inspect a journal body before confirmation. Assistant diff/table styling is presentation-only and never proves a file changed; only the closed built-in patch path can upgrade an approval preview. Theme, motion, table, command, suggestion, and picker presentation are not Session facts. The first Review remains summary-only, and Auto still avoids unknown terminals and known multiplexers until portable primary-screen evidence exists. |
| Post-v0.1 interactive model-to-human question | Fixed `packages/interaction/tool-ask-user`, `user-questions`, and Web QuestionComposer sources/tests recorded in `docs/upstream.md`; latest `master` scoped-answerer change inspected separately | Interactive-only `ask_user_question`; 1–3 bounded single-select, multi-select, or free-text questions; zero or 2–4 labelled/described options; draft-preserving backward/forward pager; final first-missing return; number-key multi toggles with user-order output; explicit custom answer beside choices; 4,096-byte trimmed custom text; exact single-select custom override and multi-select custom supplement; per-question skip encoded as empty selection; unique IDs; capacity-one whole-batch broker; per-question stale-input fence/progress; enhanced Unicode/multiline editing with next-turn-draft overlay restoration; bounded linear records; ordered all-or-nothing answers; Escape batch cancellation; compact JSON result; ordinary tool call/result Session order; turn-level wait/cancellation ownership | 26 schema/parser/broker/UI/result/editor unit tests; 12 real enhanced PTY journeys, including a three-page missing-answer/draft-retention journey; second Provider requests assert exact correlated results; `docs/design/user-question*.md`; `docs/validation/phase-{17,18,19,20,21,22}.md` | `partial` | The official UI has no equivalent three-question or 4,096-byte custom cap. Rust advertises the question tool only with an interactive answerer and bounds the human wait by the existing 30-minute turn deadline. It has one terminal answerer rather than latest `master`'s Cordis waterfall or any product subagent routing; Plan Mode's specialized single-review intent is recorded separately below. |
| Post-v0.1 bounded Goal automation | `packages/goal/command-goal`, `goal`, `goal-round-driver`, and `tool-goal` sources/tests recorded in `docs/upstream.md` at the fixed commit; latest `master` image extension inspected separately | One durable revisioned Goal; typed version-1 `goal/change` events; strict Session fold; local show/create/edit/pause/resume/clear; official Goal tool argument/result contract; exact ID/revision/round checks; configurable durable cap; model-reported blocker; sealed direct-human/Goal-round authority; tool call/change/result order; disarmed recovery with explicit resume; sequential automatic rounds; durable Ctrl+C/failure pause; Goal-round-only block threshold; post-result complete/blocked wrap-up context | Goal event/parser/codec/fold/identity/tool/caller/contract/wrap-up unit tests; real enhanced PTY auto-complete, cancellation, and create/cancel/exit/resume/show/rearm journeys with dynamic exact-ID fixtures and final-context assertions; journal order through terminal `user/message`; `docs/design/{goal-automation,goal-persistence,goal-tool-authority,goal-tool-contract,goal-wrapup}.md`; `docs/validation/phase-{12,13,14,15,16}.md` | `partial` | Rust deliberately defaults to 32 rather than 256 rounds and bounds configurable caps to positive `u32`; it accepts text only and has no Goal image attachments or autonomous background worker. Rust has only one top-level Agent, so it does not reproduce upstream root/subagent registry checks. A generated upstream-vs-Rust Goal oracle is still absent, so the row remains `partial` despite focused source-based fixtures. |
| Post-v0.1 terminal Plan Mode | Fixed `packages/plan/plan-mode`, `interaction/user-questions`, and `client/ui-user-questions` sources/tests recorded in `docs/upstream.md`; latest master image/command changes inspected separately | Durable last-value `plan/mode`; strict recovery; plan-conditioned system policy; stable `exit_plan_mode` schema; enhanced and zero-ESC linear `/plan` entry/manual exit; exact bounded Markdown review; approve/keep-planning/custom-feedback/dismiss/cancel settlement; and tool-result-before-next-step-exit ordering | Runtime/schema/codec/projection/cancellation tests; three enhanced real-PTY review journeys plus one linear entry/exit journey; exact Provider system/tool-result assertions; real JSONL ordering assertion; `docs/design/plan-mode.md`; `docs/validation/phase-23.md` | `partial` | Rust uses its existing terminal renderer rather than the Web card, has no images or product subagents, and rejects mode-changing commands during an active turn because its TUI queues next turns rather than steering the same turn. A generated cross-language oracle is still absent, so this remains `partial`. Plan Mode is guidance and cannot bypass approval. |
| Post-v0.1 model Todo list | Fixed `packages/todo/tool-todo`, core Session `todo/write`, code preset, TodoPanel, and TodoRow sources/tests recorded in `docs/upstream.md`; latest master ownership refactor inspected separately | Bounded whole-list `todo_write`; single-active policy; strict parser; durable last-value Session projection; `tool/call` → `todo/write` → `tool/result`; cancellation-before-commit guard; next-turn standing-plan lifetime; resume restoration; compact enhanced Dock summary; full zero-ESC linear list | Closed schema/parser/result unit test; Session projection test; Agent cancellation test; terminal restoration/replacement unit test; one enhanced and one linear real-PTY journey with exact Provider result and JSONL ordering; `docs/design/todo-tool.md`; `docs/validation/phase-24.md` | `partial` | Rust selects upstream's single-active configuration rather than the official code preset's parallel configuration, and adds 64-item/512-byte line bounds. It has no Web collapse control or product parallel workers. A generated cross-language oracle is absent. |
| Post-v0.1 workspace instructions | Fixed `packages/context/agent-instructions` source/README/spec/e2e and code-preset configuration recorded in `docs/upstream.md`; latest master pre-step preservation and dynamic lifecycle inspected separately | Bounded user-global/root baseline plus trusted built-in `read` and committed `apply_patch`/`str_replace_editor` nested refresh; official candidate order, same-scope trimmed dedup, framing escape and suffix-preserving budget; exact structured source; direct-input-then-context durable ordering; `tool/result` → `step/end` → next-step context order; cancellation-safe retry; visible-surface reconciliation; same-process post-compaction rearming; unavailable-source retention | Workspace discovery/render/reconcile/touch unit tests; built-in read and file-mutation provenance tests; Agent ordering, cancellation, sibling-abort, and real file-tool tests; one three-process baseline/resume PTY journey plus real nested-read and editor PTY journeys with DeepSeek-shaped requests and durable JSONL; `docs/design/{workspace-instructions,workspace-instruction-refresh,str-replace-editor}.md`; `docs/validation/phase-{25,26,32}.md` | `partial` | Rust intentionally does not walk above the opened workspace and rejects instruction symlinks. It maps upstream file mutations to committed built-in prepared tools, keeps a private trusted touch fact instead of trusting public tool names, and caps remembered nested directories at 256. Configurable candidates, restart-time recovery of a nested scope whose only visible fact was compacted, nested composite-tool bubbling, and a generated cross-language oracle remain absent. |

For the user-approved v0.1 boundary, the pressure/hard-limit one-pass summary
producer and same-input continuation are now implemented. Overflow replay,
multiple transactions, cold resident-credit parity, and the complete
physical-allocation proof remain documented hardening gaps and prevent a broad
`compatible` claim, but no longer prevent Phase 8 completion.

No row may become `compatible` without a deterministic behavioral fixture or comparison test tied to an exact upstream source or test path.
